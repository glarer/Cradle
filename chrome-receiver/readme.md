### Chrome web page as a receiver

1. Run a local webserver by "python server.py"
2. Open browser and visit "127.0.0.1:8000/receiver_log.html"

Or do not use the webserver and just open the html by browser.